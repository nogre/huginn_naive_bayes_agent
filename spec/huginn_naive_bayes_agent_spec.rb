require "spec_helper"

describe HuginnNaiveBayesAgent do
  it "has a version number" do
    expect(HuginnNaiveBayesAgent::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
